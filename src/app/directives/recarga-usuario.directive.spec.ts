import { RecargaUsuarioDirective } from './recarga-usuario.directive';

describe('RecargaUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new RecargaUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
