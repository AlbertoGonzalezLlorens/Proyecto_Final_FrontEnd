import { RecargaMapsDirective } from './recarga-maps.directive';

describe('RecargaMapsDirective', () => {
  it('should create an instance', () => {
    const directive = new RecargaMapsDirective();
    expect(directive).toBeTruthy();
  });
});
